"""Sample generation: the designs the benchmarks are run on.

Depends on `benchmarks.core` and nothing else in the package. A test enforces
that, so a DGP can never come to depend on the solver being measured.
"""
