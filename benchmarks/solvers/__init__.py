"""Adapters to the things being measured, and the settings that pin them.

Depends on `benchmarks.core` and `benchmarks.dgp`. Nothing here imports from
`benchmarks.drivers`; a test enforces that.
"""
